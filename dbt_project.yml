name: nyc_taxi
version: '1.0.0'
profile: nyc_taxi
model-paths: ["models"]

models:
  nyc_taxi:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
